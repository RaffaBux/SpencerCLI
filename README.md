# Spencer - Typescript Node React/Vite application

Start the application running:

```npm run dev```

### HTTPS

HTTPS is required for the project to work properly. To install local security certificate run 

```winget install FiloSottile.mkcert```
```mkcert -install```

then navigate to `./certs` and run

```mkcert localhost```